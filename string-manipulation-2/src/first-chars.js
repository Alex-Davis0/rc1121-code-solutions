/* exported firstChars */

// define a function named `firstChars` with two parameters `length` and `string`
// assign a method to a variable named `a`
// return `a`
function firstChars(length, string) {
  var a = string.slice(0, length);
  return a;
}

// define a function named `firstChars` with two parameters `length` and `string`
// assign a method to a variable named `a`
// return `a`

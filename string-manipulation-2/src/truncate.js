/* exported truncate */

// define a function  named `truncate` with two parameters `length` `string`
// create an expresion with a method and assign it to a variable
// return the variable

function truncate(length, string) {
  var a = string.slice(0, length) + '...';
  return a;
}

// define a function  named `truncate` with two parameters `length` `string`
// create a variable named `a` with a method and concatenate
// return the variable

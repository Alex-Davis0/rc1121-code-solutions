/* exported getValue */

// define a function named `getValue` with two parameters `object` `key`
// use a for in loop to look inside the object
// use bracket notation to get the key value

function getValue(object, key) {
  var value = object[key];
  return value;
}

// define a function named `getValue` with two parameters `object` `key`
// create a method and assign it to the variable `value`
// return `value`

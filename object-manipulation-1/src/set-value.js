/* exported setValue */

// define a function named `setValue` with three parameters `object` `key` and ` `value`
// create a variable named `a` with a method assigned to it
// return `a`

function setValue(object, key, value) {
  object[key] = value;
}

// define a function named `setValue` with three parameters `object` `key` and ` `value`
// value assigned to object at key

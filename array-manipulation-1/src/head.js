/* exported head */

// define a function named `head` with one paremeter `array`
// create a method and assign it to a variable named `a`
// return `a`

function head(array) {
  var element = array[0];
  return element;
}

// define a function named named `head` with one parameter `array`
// create a method and assign it to the variable named `element`
// reutrn `element`
